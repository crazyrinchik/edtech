/**
 * Адрес, по которому с Совёнком связываются люди.
 *
 * Один на весь продукт и в одном месте: он стоит в форме восстановления
 * пароля, в письме об обращении и в запасном пути кнопки «Сообщить об
 * ошибке» — том, который остаётся, когда почтовый сервис недоступен и
 * форма отправиться не может. Раньше адрес был вписан в разметку
 * страницы восстановления, и второе его появление стало бы второй
 * копией, которую однажды забудут поправить.
 *
 * Файл без суффикса .server: адрес попадает в разметку и должен быть
 * виден клиенту. Секрета в нём нет — он и так напечатан на странице.
 * Куда именно уходит письмо об ошибке, решает переменная SUPPORT_EMAIL
 * (см. bindings.server.ts); эта строка — умолчание и то, что человек
 * читает глазами.
 */
export const SUPPORT_EMAIL = "ekaterinazyub@gmail.com";
